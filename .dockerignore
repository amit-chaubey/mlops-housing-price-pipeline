.git
.github
.idea
.vscode
__pycache__
*.py[cod]
.ipynb_checkpoints
notebooks
data
deployment

# Streamlit is built from its own context (`./streamlit_app`)
streamlit_app

# Keep trained artifacts if present locally (git ignores `*.pkl`, but Docker may include them)
# models/**
# !models/trained/**

