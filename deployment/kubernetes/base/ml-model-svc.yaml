# Base: ML model (FastAPI) service — customize in overlays (dev/prod).
apiVersion: v1
kind: Service
metadata:
  name: ml-model
  labels:
    app: ml-model
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  selector:
    app: ml-model
