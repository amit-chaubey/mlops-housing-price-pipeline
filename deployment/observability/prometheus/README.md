# Prometheus

Metrics collection. The stack is usually installed via **kube-prometheus-stack** (see [../README.md](../README.md)).

Add custom `prometheus.yml`, scrape configs, or PrometheusRules here if you manage them outside the Helm chart.
