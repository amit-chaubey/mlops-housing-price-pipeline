# Grafana

Dashboards and alerting. The stack is usually installed via **kube-prometheus-stack** (see [../README.md](../README.md)).

Add custom datasource definitions or dashboard JSON here if you provision them separately from the Helm chart.
