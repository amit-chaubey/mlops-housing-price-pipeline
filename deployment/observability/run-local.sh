#!/usr/bin/env bash
# Quick intro script — run Prometheus + Grafana via Docker for local dev.
set -e
echo "Observability stack (placeholder). Example:"
echo "  docker run -d -p 9090:9090 prom/prometheus:latest"
echo "  docker run -d -p 3000:3000 grafana/grafana:latest"
echo "Add prometheus.yml and Grafana configs in prometheus/ and grafana/ "
